function axesOut = rcaExtra_changePlotAppearance(axesIn)

    % change legends (show on/off, change markers used, background)
    
    % change axis labels
    
    % change axis limits
    
    % changle axis fonts (size, weight, angle)
    
    % change lines (weight, line style, color, markers)

    % change errorbars for freq domain plots (color, style, weight, marker)
    
    % change shapes/confidence intervals (transparency, color)
    
    
end